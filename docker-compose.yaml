services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: cirrus
      POSTGRES_PASSWORD: cirrus
      POSTGRES_DB: cirrus
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
